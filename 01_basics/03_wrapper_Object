//All the primitive data types we have create , What happened in the Backend , A wrapper object gets created for each and every primitive data types automatically
//WHy these wrapper object gets created -> To make our life more easier so that we can use of the properties and methods which are present in the wrapper object
//We will be using ToString method to converts any data type to String, so where these methods coming from -> Wrapper objects 

let num = 10;
let str = 'String';
let bigNum = 10n;
let bool = true;

console.log(num.__proto__);
console.log(str.__proto__);
console.log(bigNum.__proto__);
console.log(bool.__proto__);

console.log(str.length);
console.log(str.indexOf("i"))
